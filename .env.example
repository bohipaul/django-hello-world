# Environment variables for Coolify deployment
# Copy this file to .env and configure your values

# Django Configuration
DJANGO_SETTINGS_MODULE=api.settings
SECRET_KEY=your-secret-key-here
DEBUG=False
ALLOWED_HOSTS=participant.devci.net

# CORS Configuration
CORS_ALLOWED_ORIGINS=https://participant.devci.net

# Database Configuration
# 🗄️ SQLite utilisé par défaut - Simple, fiable, aucune configuration requise
# - Parfait pour le développement et les déploiements simples
# - Aucune dépendance externe
# - Fichier de base de données inclus dans l'application

# Static Files
STATIC_URL=/static/
STATIC_ROOT=/app/staticfiles

# Gunicorn Configuration
PORT=3000
WEB_CONCURRENCY=4

# Security Settings for Production
SECURE_SSL_REDIRECT=True
SECURE_BROWSER_XSS_FILTER=True
SECURE_CONTENT_TYPE_NOSNIFF=True
X_FRAME_OPTIONS=DENY